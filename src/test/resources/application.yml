spring:
  profiles:
    active: test

  datasource:
    url: jdbc:h2:tcp://localhost/~/goat
    username: sa
    password: test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: false